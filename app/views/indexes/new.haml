- content_for :breadcrumbs do
  = link_to _("Indexes"), indexes_path
  &raquo;
  %b= _("New")


= form_for @index do |f|
  .form-table#index-editor
    .editor-title= _("Create Index")
    = render 'editor', f: f
