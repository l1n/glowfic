- content_for :breadcrumbs do
  = link_to _("News"), news_index_path
  &raquo;
  %b= _("New")

= form_for @news do |f|
  .form-table#news-table
    .editor-title= _("Create News Post")
    = render 'editor', f: f
