= form_for @board_section, url: board_sections_path, method: :post do |f|
  .form-table#section-editor
    .editor-title= format(_("Create %{name} Section"), name: @board_section&.board&.name)
    = render 'editor', f: f
